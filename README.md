# Mirarch — Architecture PM App

Next.js 15 App Router + Supabase + Tailwind, ported from Vite source on `main`.

## Dev

```bash
npm install
cp .env.example .env.local  # fill Supabase keys
npm run dev
```

Deployed via Vercel (region sin1) from branch `nextjs-port`.
